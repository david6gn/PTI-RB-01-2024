export interface Monitoringitem {
    sensorName: string;
    sensorValue: string;
    chartId: number;
    sensorId: string;
}

export interface sensordata {
    datetime: string;
    value: number;
}
